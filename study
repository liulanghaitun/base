至于设置屏幕的亮度，可以有以下步骤：
输入xrandr，查看输出中状态是connected的显示设备，如LVDS。具体命令可以是：
xrandr | grep -v disconnected | grep connected
调整亮度：
xrand --output LVDS --brightness 0.5
注：output后面的参数为上一步中查出的显示设备，不同主机结果可能不同。brightness后面的参数范围是0-1，0为全黑，1为最亮。

emerge --update --newuse --deep --with-bdeps=y -p @world
调节屏幕亮度:xgamma -gamma .50


桌面 gnome 再摆个conky
浏览器 主力 firefox，辅助 chromium
文档处理Libreoffice
记事 Google Keep
视频 mpv 音频 deadbeef
照片 shotwell 截图 shutter
密码管理KeepassX
游戏 Steam，已经只买不玩了
当然Wine/CrossOver也用
gnome shell的扩展，比如Top Bar等


编辑器：vim
没事喜欢修修图：gimp
矢量的话：Inkscape
截图：shutter （超赞）
Markdown编辑器：Remarkable （前一阵发现的，好漂亮） 

查看电池电量命令:acpi
十六进制编辑器:hexedit
chm查看器:xchm

114DNS 114.114.114.114 114.114.115.115
阿里DNS 223.5.5.5 223.6.6.6
百度DNS 180.76.76.76
谷歌DNS 8.8.8.8
OpenDNS 208.67.220.220
